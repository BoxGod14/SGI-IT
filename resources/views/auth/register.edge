<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>{{ t('generalPages.createAccount') }}</title>
  @entryPointStyles('scss')
</head>

<body>
  <main class="notLogged">
    <section class="container container--register">
      <h2>{{ t('generalPages.createAccount') }}</h2>    
    <form action="{{ route('AuthController.register') }}" method="post" class="registerForm">
      {{ csrfField() }}
      <div class="form-columns">
        <div>
          <section class="inputWithLabel">
            <label for="username">{{ t('generalPages.user') }}</label>
            <input type="text" name="username" 
            @if(flashMessages.has('username')) 
              value="{{flashMessages.get('username')}}"
            @end
            >
            @if(flashMessages.has('messages.username'))
            <p class="errors">{{ flashMessages.get('messages.username')}}</p>
            @end
          </section>
    
          <section class="inputWithLabel">
            <label for="email">{{ t('generalPages.mail') }}</label>
            <input type="email" name="email" 
            @if(flashMessages.has('email')) 
              value="{{flashMessages.get('email')}}" 
            @end
            >
            @if(flashMessages.has('messages.email'))
              <p class="errors">{{ flashMessages.get('messages.email')}}</p>
            @end
          </section>
    
          <section class="inputWithLabel">
            <label for="email_confirmation">{{ t('generalPages.confirmedMail') }}</label>
            <input type="email" name="email_confirmation">
            @if(flashMessages.has('messages.email_confirmation'))
            <p class="errors">{{ flashMessages.get('messages.email_confirmation')}}</p>
            @end
          </section>
          <section class="inputWithLabel">
            <label for="password">{{ t('generalPages.password') }}</label>
            <input type="password" name="password">
            @if(flashMessages.has('messages.password'))
            <p class="errors">{{ flashMessages.get('messages.password')}}</p>
            @end
          </section>  
          <section class="inputWithLabel">
            <label for="password_confirmation">{{ t('generalPages.confirmedPassword') }}</label>
            <input type="password" name="password_confirmation">
            @if(flashMessages.has('messages.password_confirmation'))
            <p class="errors">{{ flashMessages.get('messages.password_confirmation')}}</p>
            @end
          </section>  
        </div>
        
        <div>
          <section class="inputWithLabel">
            <label for="name">{{ t('generalPages.name') }}</label>
            <input type="text" name="name" 
            @if(flashMessages.has('name')) 
              value="{{flashMessages.get('name')}}" 
            @end
            >
            @if(flashMessages.has('messages.name'))
            <p class="errors">{{ flashMessages.get('messages.name')}}</p>
            @end
          </section>
    
          <section class="inputWithLabel">
            <label for="surname">{{ t('generalPages.surname') }}</label>
            <input type="text" name="surname" 
            @if(flashMessages.has('surname')) 
              value="{{flashMessages.get('surname')}}"
            @end
            >
            @if(flashMessages.has('messages.surname'))
            <p class="errors">{{ flashMessages.get('messages.surname')}}</p>
            @end
          </section>
    
          <section class="inputWithLabel">
            <label for="birthday">{{ t('generalPages.birthday') }}</label>
            <input type="date" id="birthday" name="birthday" 
            @if(flashMessages.has('birthday')) 
              value="{{flashMessages.get('birthday')}}"
            @end
            >
            @if(flashMessages.has('messages.birthday'))
            <p class="errors">{{ flashMessages.get('messages.birthday')}}</p>
            @end
          </section>        
              
          <input type="submit" value="{{ t('generalPages.createAccount') }}" class="btn_primary juan">
    
        </div>
      </div>
      
    </form>
    <p class="redirect">{{ t('generalPages.withAccount') }} <a href="{{ route('AuthController.loginForm') }}">{{ t('generalPages.login') }}</a></p>
    </section>    
  </main>
</body>

</html>